# 誤差貯金PWA v10 改善実装計画

## 方針

今回の改善は、GitHub Pagesで安定して動くPWAを保ちながら、Firebase AuthenticationとFirestoreによる任意のクラウド同期を追加する。Firebaseを設定しない場合や未ログイン時も、従来どおり端末内だけで全機能を使えることを必須とする。

文字化けを避けるため、編集対象のMarkdownとHTMLはUTF-8で扱う。日本語表示はブラウザ上で確認し、`index.html` の `meta charset="UTF-8"` を維持する。

## 対象ユーザー体験

- アプリを開いた直後に、今の誤差貯金額、目標、キャラクターの成長、記録開始がすぐ分かる。
- 上部の固定ヘッダーは置かず、スマホ画面の限られた高さをホーム内容に使う。
- 金額入力中にスマホの数字キーボードが閉じにくく、連続して記録できる。
- 記録はあとから編集・削除できる。
- カテゴリは標準カテゴリを使いつつ、自分用に追加・編集・削除できる。
- 貯まった金額で「買えるもの」が1件だけ見え、目標達成後に使う流れまで分かる。
- 貯金を使ってしまった場合も、履歴として自然に残せる。
- 古いPWAキャッシュが残っても、最新版を開ける案内と仕組みを用意する。
- メールアドレスでログインすると、記録、カテゴリ、目標、モード設定を自分のUID配下で携帯間同期できる。
- 未紐付け端末から初回ログインした場合は、同一UIDの端末データとクラウドデータをID統合し、双方を失わずクラウドへ保存する。

## 主要機能

### 1. 単一HTML PWAの安定化

- `index.html` を中心に、主要UI、状態管理、保存、画面切り替えを動かす。
- `manifest.webmanifest`、`sw.js`、アイコン参照はGitHub Pagesのルート公開で読める構成にする。
- Service Workerはキャッシュ名やURLクエリで更新を検知しやすくし、v10公開時に古いPWAキャッシュが残りにくい形にする。
- 古いPWAキャッシュ対策として、公開URLに `?v=10` を付ける手順、ホーム画面追加済みアプリの再追加案内、ブラウザキャッシュ削除案内を用意する。

### 2. 初期値と保存データ

- 初回起動時の合計貯金額、今月の貯金額、記録件数、カテゴリ別集計は0から始める。
- サンプル履歴や実績に見えるダミーデータは入れない。
- 保存先は `localStorage` とし、既存データがある場合は壊さず読み込む。
- 保存データには、記録、カテゴリ、目標、使った履歴、ユーザー設定を含める。
- アップデート後も、既存の記録、カテゴリ、目標を引き継ぎ、初期化や上書きで失わない。
- v10ではモード設定も保存対象に含める。

### 3. 記録の追加・編集・削除

- 記録データは `id`, `amount`, `categoryId`, `date`, `memo`, `createdAt`, `updatedAt` を持つ。
- ホームの大きな「誤差を記録」ボタンから記録追加へ進める。
- 記録日付はユーザーが毎回入力しなくてもよいように、自動で今日の日付を入れる。
- 履歴一覧から各記録を編集できる。
- 削除時は確認を出し、誤操作で消えないようにする。
- 編集・削除後は、合計、今月分、カテゴリ別集計、キャラクター状態、買えるもの表示を即時更新する。

### 4. カテゴリ管理

- 初期カテゴリは次の6つに統一する。
  - 交通
  - 食事
  - ショッピング
  - 固定費見直し
  - 交際費
  - その他
- カテゴリは追加・編集・削除できる。
- カテゴリ名は空文字、重複、長すぎる名前を防ぐ。
- 使用中カテゴリを削除する場合は、既存記録を「その他」へ移すか削除不可にするなど、安全な扱いを決める。
- 古いカテゴリ名が残っている既存データがあれば、表示上は現行カテゴリへ寄せる。

### 5. キャラクター画像の複数ステージ

- 合計貯金額に応じて、キャラクター画像またはキャラクター表示を複数ステージで切り替える。
- 例: 0円、1円以上、1,000円以上、10,000円以上、100,000円以上。
- ホーム内のキャラクター画像は主役になりすぎないサイズに調整し、金額や記録導線を圧迫しない。
- キャラクター画像が上下に潰れて見えないよう、縦横比を維持して表示する。
- キャラクター画像は背景なしの透過版を使い、カード背景や画面背景となじませる。
- 「現在のキャラクター」という説明ラベルは削除し、画像と成長状態だけで自然に伝える。
- 次のステージまでの目安を表示し、成長の楽しさが分かるようにする。
- ホームとマイページの両方で、同じステージ判定を使う。

### 6. 買えるもの表示

- 現在の合計貯金額で買えるものを表示する。
- 表示候補は金額しきい値で管理し、0円のときは空っぽ感ではなく「これから増える」状態にする。
- 画面上に出す買えるものは1件だけにし、一覧が長くなってホームの優先順位を崩さないようにする。
- 買えるものは目標設定と混同しない。現在買えるものは現在地、目標は行き先として見せる。

### 7. 使ってしまったボタン

- 「使ってしまった」ボタンを用意し、貯金額から使った金額を差し引けるようにする。
- 使った履歴には `id`, `amount`, `reason`, `date`, `createdAt` を持たせる。
- 合計がマイナスにならないようにする。
- 記録履歴とは別に、使った履歴も確認できるようにする。

### 8. 目標設定と達成後に使う流れ

- 目標は `targetAmount`, `targetName`, `createdAt`, `completedAt` を持つ。
- ホームに目標金額、現在の達成率、残り金額を表示する。
- 目標達成時は「目標達成して使う」ボタンを出す。
- 「目標達成して使う」を押すと、目標金額を使った履歴に移し、必要に応じて新しい目標を設定できる状態にする。
- 目標が未設定の場合は、自然に目標設定へ進める導線を置く。
- 設定済みの目標名と目標金額はあとから編集できるようにする。

### 9. マイページと統計

- マイページでは、ニックネーム、キャラクター状態、累計貯金額、総記録件数、登録カテゴリ数、目標状態を確認できる。
- マイページでは長いキャラクター説明文を置かず、状態や数字を短く確認できる構成にする。
- 統計ではカテゴリ別の合計金額、割合、記録件数を表示する。
- 記録がない場合は0表示と空状態を出す。
- マイページのカテゴリ一覧では、カテゴリ名だけを表示し、例文や用途説明は表示しない。

### 10. v8までに反映したUI修正

- 上部ヘッダーを削除し、コンテンツ開始位置を自然に詰める。
- キャラクター画像サイズを調整し、ホームの金額、目標、記録ボタンを見やすくする。
- 買えるもの表示は1件だけにする。
- マイページのキャラクター説明文はカットする。
- アップデート後も既存の記録、カテゴリ、目標を引き継ぐ。
- v8までの修正は公開済みの前提とし、今回の公開確認はv9の確認URLを使う。
- 古いPWAキャッシュが残る場合の再読み込み、キャッシュ削除、ホーム画面アイコン再追加の案内を維持する。

### 11. v9追加修正

- キャラクター画像の上下潰れを直し、縦横比を保って表示する。
- キャラクター画像は背景なしの透過版を使う。
- ホームやマイページに残っている「現在のキャラクター」文字を削除する。
- 記録日付は自動で今日の日付にする。
- 目標名と目標金額をあとから編集できる機能を追加する。
- v9として公開し、確認URLは `https://bonjouryamada.github.io/gosa-savings/?v=9` を使う。
- 古いPWAキャッシュが残る場合に備え、Service Workerのキャッシュ名、参照URL、公開確認URLをv9向けに更新する。

### 12. v10 Firebase Authentication

- メールアドレスとパスワードによる新規登録、ログイン、ログアウトを用意する。
- 認証状態を監視し、ログイン中のメールアドレスと同期状態を分かりやすく表示する。
- Firebase設定値が未入力、SDK読込失敗、オフライン、未ログインのいずれでもアプリ本体を止めず、端末内保存で継続する。
- 認証エラーは日本語で案内し、パスワードや秘密情報をログ・Firestore・`localStorage` に保存しない。
- Firebaseプロジェクト作成やAuthentication、Firestore、ルール設定は外部サービス側の作業であり、ユーザー本人の対応が必要であることを明記する。

### 13. v10 Firestore同期とデータ保護

- FirestoreではユーザーUID単位でデータを分離し、他ユーザーのデータを読み書きできない構成とルールにする。
- 同期対象は記録、カテゴリ、目標、モード設定とする。端末内には従来データを維持し、クラウド障害時も閲覧・記録を継続できるようにする。
- 各データに安定したIDと更新日時を持たせ、同期時の重複登録を防ぐ。
- 未紐付け端末または同一UIDへのログインでは、`records` と `categories` をID単位で統合し、端末・クラウド双方のデータを失わずクラウドへ保存する。
- 異なるUIDへ切り替えた場合は、旧UIDの端末データを新UIDへ混在させず、新UIDのクラウド状態へ切り替える。
- 同期失敗時は端末データを消さず、再同期できる状態を保つ。
- ログアウト後はクラウド同期を停止し、別UIDのデータを画面に混在させない。

### 14. v10 モード設定

- ノーマルモードでは「使ってしまった」ボタンと関連導線を非表示にする。
- ストイックモードでは「使ってしまった」ボタンと関連導線を表示する。
- 初期値はノーマルモードとし、モード切替は設定画面で明示的に行う。
- モード設定は未ログイン時は端末内、ログイン時はUID単位でFirestoreにも保存・同期する。
- モードを切り替えても既存の使った履歴は削除しない。

### 15. v10 成長段階とキャラクターレイアウト

- 成長段階を0円から1億円まで再設計し、金額しきい値は単調増加かつ最終段階を1億円到達にする。
- 段階名には順番に「わかば」「つぼみ」「あさがお」「さくら」「ガジュマル」「縄文杉」「木遁・樹海降誕」「ユグドラシル」を含める。
- 各段階の現在地、次の段階名、次段階までの残額を同じ判定ロジックで表示する。
- キャラクターは金額・目標・主要操作を圧迫しない位置へ再配置し、ホームとマイページで用途に合うサイズにする。
- 画像は `object-fit: contain` 等で元の縦横比を維持し、固定高さによる潰れ、過度な切り抜き、レイアウトの跳ねを防ぐ。

### 16. v10 公開

- Firebase用の設定ファイル、Firestoreセキュリティルール、初心者向け `FIREBASE_SETUP.md` を公開物と手順に含める。
- v10成果物には `growth-stages.js`、`firebase-config.js`、`firebase-config.example.js`、`firebase-sync.js`、`firestore.rules`、25点のキャラクター画像素材、v10公開フォルダ、v10配布ZIPを含める。
- v10として公開し、確認URLは `https://bonjouryamada.github.io/gosa-savings/?v=10` を使う。
- Service Workerのキャッシュ名、参照URL、公開確認URLをv10向けに更新する。

## 検証方針

- ローカルで `index.html` を開き、初期表示が0円・空履歴になっていることを確認する。
- 記録追加、編集、削除が反映されることを確認する。
- カテゴリ追加、編集、削除が保存され、再読み込み後も残ることを確認する。
- 現行カテゴリ名が「交通、食事、ショッピング、固定費見直し、交際費、その他」になっていることを確認する。
- キャラクターの複数ステージ、買えるもの表示、使ってしまったボタン、目標設定、目標達成して使うボタンを確認する。
- スマホ幅で金額入力時に数字キーボードが使いやすいことを確認する。
- GitHub Pages公開後、通常URLと `?v=10` 付きURLで表示を確認する。
- キャラクター画像が上下に潰れておらず、背景なしの透過版で表示されることを確認する。
- 「現在のキャラクター」文字が表示されていないことを確認する。
- 記録追加時の日付が今日の日付で自動設定されることを確認する。
- 目標編集後、目標名、目標金額、達成率、残り金額が更新されることを確認する。
- 古いホーム画面PWAが白画面や旧画面を出す場合は、再追加手順で解消することを確認する。
- Firebase未設定、未ログイン、オフライン、同期失敗時にも端末内保存で記録を継続できることを確認する。
- メール/パスワード登録、ログイン、ログアウトと、同一UIDでの携帯間同期を確認する。
- 未紐付け端末・同一UIDでは `records` と `categories` がID統合され、端末・クラウド双方のデータが失われないことを確認する。
- 異なるUIDへの切替時は旧UID端末データが混在せず、新UIDクラウド状態へ切り替わることを確認する。
- 別UID間でデータが混在せず、Firestoreルールで他UIDの読み書きが拒否されることを確認する。
- ノーマルモードで「使ってしまった」が非表示、ストイックモードで表示されることを確認する。
- マイページのカテゴリ一覧に例文が表示されないことを確認する。
- 0円から1億円までの全成長段階名、しきい値、キャラクター配置、縦横比を確認する。
- Node構文、manifest、成長境界、画像透過・寸法、静的参照の検証を行う。
- アプリ内ブラウザのローカルURLは保護機能で遮断されたため、Browser visual testは未完了として実機または公開URLで確認する。

## 担当・注意

- この計画は全体方針とTODOを示すための文書。
- Worker Planは `implementation-plan.md`, `todo.md`, `PUBLISH_TO_GITHUB.md`, `FIREBASE_SETUP.md` のみを更新する。
- 他Workerが担当する実装ファイルや公開用フォルダは戻さない。
## v11 追加計画（成長段階・公開・収益化方針）

- 最終成長段階「ユグドラシル」の到達額を、従来の1億円から1,000万円へ変更する。
- 成長段階は全25段階を維持する。0円〜90万円の段階と到達額は現行のまま変更せず、100万円以降の段階を1,000万円まで自然な間隔になるよう圧縮する。
- 実装時は段階判定、次段階までの残額、ホームとマイページの表示が同じ25段階定義を参照する状態を維持する。
- v11の公開確認URLは `https://bonjouryamada.github.io/gosa-savings/?v=11` とする。
- Service Workerの予定キャッシュ名・参照URLをv11へ更新し、公開パッケージ名もv11とする。
- 今回のv11では収益化機能を実装しない。別途検討事項として、広告、応援プラン、プレミアム機能を比較検討する。
- 収益化や分析を将来導入する場合も、貯金金額、記録メモ、メールアドレス等の個人データを広告事業者または分析サービスへ送信しないことをプライバシー原則とする。
- 外部Firebase設定、GitHub公開、公開後の実機確認はユーザー対応を要するため、未完了のまま継続する。
