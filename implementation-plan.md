# 誤差貯金アプリ 指摘対応 実装計画

## 方針

今回の修正は、GitHub Pagesで安定して動く単一HTML PWAとしてまとめ直す。ビルド工程やサーバー前提を増やさず、`index.html` を中心に、必要なCSS、JavaScript、PWA設定、ローカル保存処理を同一ページ内または既存の静的ファイル参照だけで完結させる。

文字化けを避けるため、すべての編集ファイルはUTF-8で扱い、`index.html` の先頭に `meta charset="UTF-8"` がある状態を維持する。日本語文言はブラウザ表示で確認し、不要な記号や環境依存文字を増やさない。

## 対象ユーザー体験

- アプリを開いた直後、謎のスマホ風ステータス表示ではなく、誤差貯金の状態と記録開始がすぐ見える。
- 初期状態では金額、統計、履歴、カテゴリ別集計がすべて0から始まり、ダミーデータが実績のように見えない。
- 金額入力中にキーボードが勝手に閉じず、スマホでも続けて入力しやすい。
- ホームは小さなクイック金額ボタンではなく、大きな入力ボタンから記録に進める。
- カテゴリは実際の節約行動に合う構成へ刷新し、ユーザーがカテゴリを追加できる。
- キャラクターアイコンと成長演出が貯金額の桁に応じて変化し、続ける楽しさがある。
- マイページではユーザー情報、キャラクター状態、累計、設定が自然に確認できる。
- 統計では合計だけでなく、カテゴリ別の貯金傾向が読める。

## 実装範囲

### 1. 単一HTML PWAとしての安定化

- `index.html` 単体で主要UI、状態管理、保存、画面切り替えが動く構成を守る。
- GitHub Pagesでパスが崩れないよう、ルーティングはハッシュまたは同一ページ内の状態切り替えにする。
- Service Worker、manifest、アイコン参照がある場合は、相対パスでGitHub Pages配下でも読める形にする。
- 外部ビルドツール、Node依存、APIサーバー依存を追加しない。

### 2. 初期値を0に統一

- 初回起動時の合計貯金額、今月の貯金額、記録回数、カテゴリ別統計、履歴件数を0にする。
- デモ用の履歴やサンプル金額がある場合は削除する。
- 空状態では、実績に見える数値ではなく「まだ記録がありません」という案内にする。

### 3. 謎の時刻・電波マーク削除

- アプリ内に固定表示されているスマホステータス風の時刻、電波、Wi-Fi、バッテリー表現を削除する。
- 実端末のブラウザUIと重複して見える装飾をなくし、画面上部はアプリ名、キャラクター、主要残高に集中させる。

### 4. 入力設計の修正

- 金額入力は `inputmode="numeric"` または `type="tel"` を使い、スマホで数字キーボードを出す。
- 入力値の整形は入力中にフォーカスを奪わず、保存時または `blur` 時に行う。
- 画面切り替え、カテゴリ選択、金額ボタン操作で金額入力欄が意図せず再生成されないようにする。
- 保存後だけフォームを閉じる、または入力完了が分かる状態にする。

### 5. ホーム導線の変更

- ホーム上のクイック金額ボタンを削除する。
- 代わりに、親指で押しやすい大きな「誤差を記録」ボタンを主要導線として配置する。
- 合計貯金額、キャラクター、成長状態、記録ボタンの順に視線が流れるようにする。

### 6. カテゴリ刷新と追加機能

- 既存カテゴリを、日常の誤差貯金に合うカテゴリへ見直す。
- 初期カテゴリ案: 食費、飲み物、交通、買い物、娯楽、固定費、その他。
- カテゴリ選択UIには、選択中が分かる状態を用意する。
- ユーザーがカテゴリ名を追加できる入力欄または追加ダイアログを用意する。
- 追加カテゴリは `localStorage` に保存し、再読み込み後も残す。
- 空文字、重複、長すぎるカテゴリ名を防ぐ。

### 7. キャラクターアイコンと桁連動の進化

- ホームとマイページにキャラクターアイコンを表示する。
- 合計貯金額の桁に応じてキャラクター段階を変える。
- 段階案:
  - 0円: たまご、または眠っている初期状態
  - 1円から999円: レベル1
  - 1,000円から9,999円: レベル2
  - 10,000円から99,999円: レベル3
  - 100,000円以上: レベル4
- 進化条件は表示文言にも反映し、次の桁までの目安を出す。
- アイコンは文字化けしない方法で実装する。絵文字を使う場合は表示確認し、崩れる場合はCSS図形または既存画像に切り替える。

### 8. マイページ修正

- マイページは設定置き場だけでなく、ユーザーの積み上げを確認する画面にする。
- 表示項目: 名前またはニックネーム、キャラクター状態、累計貯金額、総記録回数、カテゴリ数、データ管理。
- データ管理には、必要に応じて全削除、エクスポート準備、PWA案内を置く。
- 実装済みでない外部連携やストア機能を実装済みのように見せない。

### 9. カテゴリ別統計

- 統計画面にカテゴリ別合計を追加する。
- 表示項目: カテゴリ名、合計金額、全体に占める割合、記録回数。
- 記録がない場合は0表示と空状態を出す。
- グラフを入れる場合も、単一HTMLで壊れにくいCSSバー表現を優先する。

## データ設計

- 記録データ: `amount`, `category`, `date`, `memo`, `createdAt` を持つ配列として保存する。
- カテゴリデータ: 初期カテゴリとユーザー追加カテゴリを統合して扱う。
- 保存先は `localStorage` とし、キー名は既存実装があればそれに合わせる。
- 初期化時は保存済みデータがない場合だけ空配列と初期カテゴリを使う。

## 検査方針

- ローカルで `index.html` を開き、初期値が0で表示されることを確認する。
- ローカルサーバーで表示し、Service Workerやmanifestの参照エラーがないことを確認する。
- 文字化けがないことを、ファイル内容とブラウザ表示の両方で確認する。
- スマートフォン幅で、金額入力中にキーボードが閉じない設計になっていることを確認する。
- 記録追加後、ホーム、履歴、カテゴリ別統計、マイページ、キャラクター進化が同時に更新されることを確認する。
- GitHub Pages公開後、公開URLでリロード、画面切り替え、PWA manifest、アイコン参照が壊れていないことを確認する。

## 担当分担の注意

- この計画は全体方針とTODOを示すためのもの。実装担当workerは、自分の担当ファイル以外を戻したり上書きしたりしない。
- 他workerの変更が入った場合は、最新ファイルを読んでから自分の担当範囲だけを追記・修正する。
- 完了報告では、変更ファイル、確認した項目、未確認の項目を分けて報告する。
