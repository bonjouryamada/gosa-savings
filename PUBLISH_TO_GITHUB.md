# GitHub Pagesに公開する手順

この手順は、コマンドを使わずにGitHubの画面だけで公開する方法です。

## 使うもの

最新版フォルダ、または最新版ZIPを使います。

ZIPの場合は、先に展開してください。GitHubにアップロードするのはZIPファイルそのものではなく、展開したフォルダの中にあるファイルとフォルダです。

重要: フォルダごと入れるのではなく、最新版フォルダを開いた中身をすべてアップロードします。

## 公開前に必要なFirebase設定

v10の携帯間同期を使うには、GitHubへのアップロードとは別にFirebase側の設定が必要です。Firebaseは外部サービスのため、この作業はプロジェクト所有者であるユーザー本人が行う必要があります。

詳しい手順は `FIREBASE_SETUP.md` を参照してください。Firebaseを設定しなくても、アプリは従来どおり端末内保存で利用できます。

FirebaseのWeb設定値はブラウザ用クライアント設定なので、公開リポジトリに置くこと自体は通常の構成です。ただし、`firestore.rules` で本人UIDだけにアクセスを制限し、Firebase Consoleへ公開することが必須です。パスワードなどの秘密情報は絶対にファイルへ保存・公開しないでください。

## アップロード手順

1. GitHubで `bonjouryamada/gosa-savings` を開く。
2. `Add file` を押す。
3. `Upload files` を押す。
4. 最新版フォルダを開く。
5. 中にあるファイルとフォルダをすべて選ぶ。
6. GitHubのアップロード画面へドラッグ&ドロップする。
7. 画面下の `Commit changes` を押す。
8. 2〜3分待つ。
9. 公開URLを開く。

```text
https://bonjouryamada.github.io/gosa-savings/
```

## 最新版を確実に見る方法

古いPWAキャッシュが残っていると、アップロード後も前の画面や白い画面が出ることがあります。その場合は、URLの最後に確認用の番号を付けて開きます。

```text
https://bonjouryamada.github.io/gosa-savings/?v=10
```

今回はv10公開なので、確認URLは次を使います。

```text
https://bonjouryamada.github.io/gosa-savings/?v=10
```

次の公開では、最後の数字を前回と違う番号にします。

## まだ古い画面が出る場合

- PCではシークレットウィンドウで開く。
- スマホでは別のブラウザで一度開く。
- URLに `?v=10` を付けて開く。
- ブラウザのキャッシュを削除する。
- ホーム画面に追加済みの古いPWAは、一度削除してから追加し直す。

## ホーム画面に追加済みの場合

すでにスマホのホーム画面へ追加している古い「誤差貯金」は、前のService Workerやキャッシュを持っていることがあります。

最新版に切り替わらない場合は、ホーム画面の古いアイコンを削除してから、公開URLをブラウザで開き直し、もう一度ホーム画面に追加してください。

## GitHub Pages設定の確認

公開されない場合は、GitHubのリポジトリで次を確認します。

1. `Settings` を開く。
2. `Pages` を開く。
3. `Source` が `Deploy from a branch` になっていることを確認する。
4. `Branch` が `main` になっていることを確認する。
5. フォルダが `/ (root)` になっていることを確認する。
6. 必要なら `Save` を押す。

## 公開後の確認

- 画面が白くならない。
- 日本語が文字化けしていない。
- 記録追加、編集、削除ができる。
- カテゴリ追加、編集、削除ができる。
- キャラクター画像の複数ステージが表示される。
- 買えるもの表示が出る。
- 「使ってしまった」ボタンが使える。
- 目標設定と「目標達成して使う」ボタンが使える。
- Firebase未設定または未ログインでも端末内保存が使える。
- メール/パスワード登録、ログイン、ログアウトができる。
- ログイン時に記録、カテゴリ、目標、モード設定が同じUIDの携帯間で同期される。
- 未紐付け端末・同一UIDでは、記録とカテゴリがID統合され双方のデータが残る。
- 異なるUIDへの切替時は、旧UID端末データが混在せず新UIDクラウド状態へ切り替わる。
- ノーマルモードでは「使ってしまった」が非表示、ストイックモードでは表示される。
- マイページのカテゴリ一覧に例文が表示されない。
- 成長段階が0円から1億円まで切り替わり、キャラクター画像が潰れない。
- manifest、Service Worker、アイコンが読み込まれている。
- v10確認URL `https://bonjouryamada.github.io/gosa-savings/?v=10` で最新版が表示される。
## v11 公開時の追加事項

- v11公開パッケージを使用し、パッケージ名にv11を明記する。
- Service Workerの予定キャッシュ名と参照URLがv11になっていることを公開前に確認する。
- 最新版確認URLは次を使用する。

```text
https://bonjouryamada.github.io/gosa-savings/?v=11
```

- GitHubへのアップロードとGitHub Pages公開は未完了の外部作業として残す。
- 公開後のPC、スマートフォン、ホーム画面追加済みPWAでの実機確認も未完了のまま残す。
- v11では収益化を実装しない。将来の広告・応援プラン・プレミアム機能を検討する場合も、貯金金額、記録メモ、メールアドレス等を広告事業者または分析サービスへ送信しない。

## v12 公開時の追加事項

- v12は無料運用を優先し、Firebase同期を使わない端末内保存版として公開する。
- `index.html` では `firebase-config.js` と `firebase-sync.js` を読み込まない。
- マイページの表示は「設定・端末内保存」とし、メール登録・ログイン・クラウド同期ボタンは表示しない。
- Service Workerのキャッシュ名は `gosa-savings-app-v12` を使用する。
- 最新版確認URLは次を使用する。

```text
https://bonjouryamada.github.io/gosa-savings/?v=12
```

- Firebase関連ファイルは将来の同期機能再開用に残すが、v12のアプリ本体からは利用しない。
