# 誤差貯金PWA v10 改善TODO

## TODO 1: 計画と担当範囲

- [x] 追加要望を実装計画に反映する。
- [x] Worker Planの担当ファイルを `implementation-plan.md`, `todo.md`, `PUBLISH_TO_GITHUB.md`, `FIREBASE_SETUP.md` に限定する。
- [x] 日本語が文字化けしないUTF-8のMarkdownとして整える。
- [x] 古いカテゴリ名を現行カテゴリへ統一する。

## TODO 2: GitHub Pages向けPWA安定化

- [ ] `index.html` 単体で主要UI、保存、画面切り替えが動くことを確認する。
- [ ] `manifest.webmanifest`、`sw.js`、アイコン参照が公開URLで読めることを確認する。
- [x] Service Workerのキャッシュ名または参照URLをv10向けに更新し、古いPWAキャッシュが残りにくい形にする。
- [x] 公開確認用URLには `?v=10` を付ける。
- [x] ホーム画面追加済みPWAが古い場合の再追加手順を案内する。
- [ ] v10として公開する。

## TODO 3: 初期値と保存データ

- [x] 初回起動時の合計貯金額を0円にする。
- [x] 今月の貯金額を0円にする。
- [x] 記録件数を0件にする。
- [x] カテゴリ別集計を0から始める。
- [x] サンプル履歴や実績に見えるダミーデータを削除する。
- [x] `localStorage` に記録、カテゴリ、目標、使った履歴、ユーザー設定を保存する。
- [x] アップデート後も既存の記録を引き継ぐ。
- [x] アップデート後も既存のカテゴリを引き継ぐ。
- [x] アップデート後も既存の目標を引き継ぐ。

## TODO 4: 記録の追加・編集・削除

- [ ] 大きな「誤差を記録」ボタンから記録追加へ進める。
- [x] 記録に金額、カテゴリ、日付、メモを保存する。
- [x] 記録日付は自動で今日の日付にする。
- [x] 履歴一覧から記録を編集できるようにする。
- [x] 履歴一覧から記録を削除できるようにする。
- [x] 削除前に確認を出す。
- [ ] 編集・削除後に合計、今月分、カテゴリ別集計、キャラクター状態、買えるもの表示を更新する。

## TODO 5: 入力しやすさ

- [x] 金額入力に `inputmode="numeric"` または `type="tel"` を設定する。
- [x] 入力中にフォーカスが不用意に外れないようにする。
- [x] カテゴリ選択や画面切り替えで金額入力欄が勝手に再生成されないようにする。
- [x] 保存後は入力欄をクリアする。
- [ ] スマホ幅で連続入力しやすいことを確認する。

## TODO 6: ホーム導線

- [x] 上部ヘッダーを削除する。
- [x] ホームの小さなクイック金額ボタンを削除する。
- [ ] 大きな「誤差を記録」ボタンを主要導線として配置する。
- [ ] 合計貯金額、目標、キャラクター、記録ボタンの視線順を整える。
- [ ] 親指で押しやすいボタンサイズと位置にする。

## TODO 7: カテゴリ管理

- [x] 初期カテゴリを「交通、食事、ショッピング、固定費見直し、交際費、その他」に統一する。
- [x] カテゴリ名が「交通、食事、ショッピング、固定費見直し、交際費、その他」に統一されていることを確認する。
- [x] カテゴリ追加UIを用意する。
- [x] カテゴリ編集UIを用意する。
- [x] カテゴリ削除UIを用意する。
- [x] 追加・編集したカテゴリを `localStorage` に保存する。
- [ ] 再読み込み後もカテゴリが残ることを確認する。
- [x] 空文字、重複、長すぎるカテゴリ名を登録できないようにする。
- [x] 使用中カテゴリを削除するときの安全な扱いを実装する。

## TODO 8: キャラクター画像の複数ステージ

- [x] 0円の初期キャラクター表示を用意する。
- [ ] 1円以上のステージ表示を用意する。
- [x] 1,000円以上のステージ表示を用意する。
- [x] 10,000円以上のステージ表示を用意する。
- [x] 100,000円以上のステージ表示を用意する。
- [x] 合計貯金額に応じてステージを切り替える。
- [ ] キャラクター画像サイズを調整し、ホームの主要情報を圧迫しないようにする。
- [x] キャラクター画像の上下潰れを修正し、縦横比を維持する。
- [x] キャラクター画像は背景なしの透過版を使う。
- [x] 「現在のキャラクター」文字を削除する。
- [x] 次のステージまでの目安を表示する。
- [x] ホームとマイページで同じステージ判定を使う。

## TODO 9: 買えるもの表示

- [ ] 現在の合計貯金額で買えるものを表示する。
- [ ] 買えるもの候補を金額しきい値で管理する。
- [ ] 画面に表示する買えるものは1件だけにする。
- [ ] 0円のときも自然な空状態を表示する。
- [ ] 記録追加、編集、削除、使った履歴追加のあとに表示を更新する。
- [ ] 目標表示と混同しない文言にする。

## TODO 10: 使ってしまったボタン

- [x] 「使ってしまった」ボタンを用意する。
- [x] 使った金額と理由を入力できるようにする。
- [x] 使った履歴を保存する。
- [x] 合計貯金額から使った金額を差し引く。
- [x] 合計がマイナスにならないようにする。
- [x] 使った履歴をあとから確認できるようにする。

## TODO 11: 目標設定

- [x] 目標名を設定できるようにする。
- [x] 目標金額を設定できるようにする。
- [x] ホームに達成率と残り金額を表示する。
- [x] 目標が未設定の場合は目標設定への導線を出す。
- [x] 目標を編集できるようにする。
- [x] 目標編集後、目標名、目標金額、達成率、残り金額を更新する。
- [x] 目標を削除またはリセットできるようにする。

## TODO 12: 目標達成して使う

- [x] 目標達成時に「目標達成して使う」ボタンを表示する。
- [x] ボタン押下時に目標金額を使った履歴へ移す。
- [x] 合計貯金額から目標金額を差し引く。
- [ ] 達成日時を保存する。
- [x] 次の目標を設定しやすい状態にする。

## TODO 13: マイページと統計

- [ ] マイページにニックネームまたはプロフィール表示を用意する。
- [x] キャラクター状態を表示する。
- [x] マイページの長いキャラクター説明文をカットする。
- [x] 累計貯金額を表示する。
- [x] 総記録件数を表示する。
- [x] 登録カテゴリ数を表示する。
- [x] 目標状態を表示する。
- [ ] 統計画面にカテゴリ別合計、割合、記録件数を表示する。
- [x] 記録がない場合は0表示と空状態を出す。

## TODO 14: ローカル確認

- [ ] `index.html` をローカルで開き、初期表示が0円・空履歴になっていることを確認する。
- [ ] 上部ヘッダーが表示されていないことを確認する。
- [ ] キャラクター画像サイズが大きすぎないことを確認する。
- [ ] キャラクター画像が上下に潰れていないことを確認する。
- [ ] キャラクター画像が背景なしの透過版で表示されることを確認する。
- [x] 「現在のキャラクター」文字が表示されていないことを確認する。
- [ ] 記録追加、編集、削除を確認する。
- [ ] 記録追加時の日付が今日の日付で自動設定されることを確認する。
- [ ] カテゴリ追加、編集、削除を確認する。
- [x] キャラクター複数ステージを確認する。
- [ ] 買えるもの表示が1件だけであることを確認する。
- [ ] 使ってしまったボタンを確認する。
- [ ] 目標設定と目標達成して使うボタンを確認する。
- [ ] 目標編集機能を確認する。
- [ ] アップデート前の記録、カテゴリ、目標が引き継がれることを確認する。
- [ ] マイページの長いキャラクター説明文が表示されていないことを確認する。
- [x] 日本語が文字化けしていないことを確認する。
- [ ] スマホ幅でレイアウトの重なりがないことを確認する。

## TODO 15: GitHubアップロードと公開確認

- [x] 最新版フォルダまたは最新版ZIPの中身を確認する。
- [ ] GitHubの画面から最新版フォルダの中身をアップロードする。
- [ ] GitHub Pagesの公開設定が `main` / `/(root)` になっていることを確認する。
- [ ] 公開URLを通常表示で開く。
- [ ] 公開URLを `?v=10` 付きで開く。
- [ ] manifest、Service Worker、アイコンが公開URLで読めることを確認する。
- [ ] ホーム画面追加済みPWAが古い場合は削除して追加し直す。
- [ ] 確認結果と残課題を報告する。

## TODO 16: Firebase Authentication

- [x] メールアドレスとパスワードで新規登録できるようにする。
- [x] メールアドレスとパスワードでログインできるようにする。
- [x] ログアウトできるようにする。
- [x] 認証状態、ログイン中メールアドレス、同期状態を表示する。
- [x] Firebase未設定、SDK読込失敗、未ログイン、オフライン時も端末内保存で全機能を継続する。
- [x] 認証エラーを日本語で表示し、パスワードを保存・出力しない。

## TODO 17: Firestore UID単位同期

- [x] 記録をログイン中UID単位で保存・同期する。
- [x] カテゴリをログイン中UID単位で保存・同期する。
- [x] 目標をログイン中UID単位で保存・同期する。
- [x] モード設定をログイン中UID単位で保存・同期する。
- [x] 安定したIDと更新日時で重複登録を防ぐ。
- [x] 同期失敗時も端末データを削除せず、再同期できるようにする。
- [x] ログアウトやUID切替時に別ユーザーのデータを混在させない。
- [x] `firestore.rules` に本人UID配下だけ読み書きできるルールを実装する。

## TODO 18: 初回ログイン時の安全な移行

- [x] 未紐付け端末・同一UIDでは `records` と `categories` をID統合する。
- [x] 端末・クラウド双方のデータを失わずクラウドへ保存する。
- [x] 異なるUIDへの切替時は旧UID端末データを混在させず、新UIDクラウド状態へ切り替える。
- [x] 同期失敗後も端末データを保持する。

## TODO 19: モードとマイページ

- [x] 初期値をノーマルモードにする。
- [x] ノーマルモードでは「使ってしまった」と関連導線を非表示にする。
- [x] ストイックモードでは「使ってしまった」と関連導線を表示する。
- [x] モード切替後も既存の使った履歴を削除しない。
- [x] マイページのカテゴリ一覧から例文・用途説明を非表示にする。

## TODO 20: 成長段階とキャラクター再設計

- [x] 0円から1億円までの成長しきい値を単調増加で定義する。
- [x] 「わかば」「つぼみ」「あさがお」「さくら」「ガジュマル」「縄文杉」「木遁・樹海降誕」「ユグドラシル」を段階名に含める。
- [x] 現在段階、次段階、次段階までの残額を共通ロジックで表示する。
- [x] キャラクターを主要情報と操作を圧迫しない位置へ再配置する。
- [x] ホームとマイページに適した画像サイズを定義する。
- [x] 全段階で画像の縦横比を維持し、潰れ・切り抜き・レイアウトの跳ねがないことを静的検証する。

## TODO 21: Firebase設定とv10公開

- [x] 初心者向け `FIREBASE_SETUP.md` を作成する。
- [x] `growth-stages.js`、Firebase関連ファイル、25点の画像素材、v10公開フォルダ・ZIPを成果物へ含める。
- [x] Node構文、manifest、成長境界、画像透過・寸法、静的参照を検証する。
- [ ] Browser visual testを実施する。アプリ内ブラウザのローカルURLは保護機能で遮断されたため未完了。
- [ ] FirebaseプロジェクトとWebアプリを作成する。これは外部サービス側でユーザー対応が必要。
- [ ] Email/Password認証を有効化する。これは外部サービス側でユーザー対応が必要。
- [ ] Firestoreデータベースを作成する。これは外部サービス側でユーザー対応が必要。
- [ ] Firebase設定値を `firebase-config.js` に貼り付ける。これはユーザー対応が必要。
- [ ] `firestore.rules` をFirebaseへ設定・公開する。これは外部サービス側でユーザー対応が必要。
- [x] Firebase未設定状態をローカル検証する。
- [ ] Firebase設定済み状態を実機検証する。ユーザー対応が必要。
- [ ] v10公開物をアップロードし、`https://bonjouryamada.github.io/gosa-savings/?v=10` で確認する。
## TODO 22: v11 成長段階・公開パッケージ更新

- [x] 25段階を維持し、0円〜90万円は現行の到達額を変更せず、100万円以降を最終段階ユグドラシル1,000万円到達まで自然に圧縮して実装する。
- [x] 90万円直前・90万円・100万円直前・100万円・各圧縮後しきい値・1,000万円直前・1,000万円・1,000万円超の境界値テストを行う。
- [x] 段階判定、次段階までの残額、ホームとマイページの表示が同じ25段階定義を使うことを確認する。
- [x] Service Workerの予定キャッシュ名と参照URLをv11へ更新する。
- [x] v11公開パッケージを作成・更新する。
- [ ] 公開確認URL `https://bonjouryamada.github.io/gosa-savings/?v=11` で確認する。
- [ ] 外部Firebase設定を完了する。
- [ ] GitHubへv11公開パッケージを公開する。
- [ ] 公開後にPC・スマートフォン・ホーム画面追加済みPWAで実機確認する。

## TODO 23: 収益化の別途検討

- [ ] 広告の採用可否と表示方針を検討する。v11では実装しない。
- [ ] 応援プランの採用可否と提供方法を検討する。v11では実装しない。
- [ ] プレミアム機能の候補、価格、無料機能との境界を検討する。v11では実装しない。
- [ ] 貯金金額、記録メモ、メールアドレス等を広告事業者または分析サービスへ送信しないプライバシー原則を維持する。
