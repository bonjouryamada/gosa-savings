# GitHub Pagesで公開する手順

この手順は、公開リポジトリを使って、Gitやコマンドラインを使わずにWebアプリを公開する方法です。

公開後のURLは、たとえば次のようになります。

`https://ユーザー名.github.io/gosa-savings/`

## 1. GitHubにログインする

1. ブラウザで [GitHub](https://github.com/) を開きます。
2. 右上の **Sign in** を押します。
3. GitHubアカウントのメールアドレスまたはユーザー名と、パスワードを入力してログインします。

まだアカウントがない場合は、**Sign up** から新しく作成してください。

## 2. 新しいリポジトリを作る

1. GitHubの画面右上にある **+** を押します。
2. **New repository** を選びます。
3. **Repository name** に `gosa-savings` と入力します。
4. 公開する前提なので、**Public** を選びます。
5. **Add a README file** は、必要ならチェックを入れても大丈夫です。
6. **Create repository** を押します。

これで、公開用の置き場所が作成されます。

## 3. ファイルをアップロードする

1. 作成した `gosa-savings` リポジトリの画面を開きます。
2. **Add file** を押します。
3. **Upload files** を選びます。
4. 公開したいファイルをドラッグ&ドロップします。

最低限、次のようなファイルをアップロードします。

- `index.html`
- `styles.css`
- `app.js`
- `manifest.webmanifest`
- `sw.js`
- `public` フォルダ内の画像やアイコン

アップロード画面の下にある **Commit changes** を押すと、ファイルが保存されます。

## 4. GitHub Pagesを有効にする

1. リポジトリ画面の上部にある **Settings** を押します。
2. 左側メニューの **Pages** を押します。
3. **Build and deployment** の **Source** で **Deploy from a branch** を選びます。
4. **Branch** で `main` を選びます。
5. フォルダは `/ (root)` を選びます。
6. **Save** を押します。

これで、GitHub Pagesで公開する設定が完了します。

## 5. 公開URLを開く

設定後、公開までに少し時間がかかることがあります。数十秒から数分ほど待ってから、次のURLを開きます。

`https://ユーザー名.github.io/gosa-savings/`

`ユーザー名` の部分は、自分のGitHubユーザー名に置き換えてください。

ページが開けない場合は、少し待ってから再読み込みしてください。GitHubの **Settings > Pages** に表示されるURLから開くこともできます。

## 6. スマホでホーム画面に追加する

公開URLをスマホで開くと、アプリのようにホーム画面へ追加できます。

### iPhoneの場合

1. Safariで公開URLを開きます。
2. 画面下の共有ボタンを押します。
3. **ホーム画面に追加** を選びます。
4. 名前を確認して **追加** を押します。

### Androidの場合

1. Chromeで公開URLを開きます。
2. 右上のメニューを押します。
3. **ホーム画面に追加** または **アプリをインストール** を選びます。
4. 名前を確認して追加します。

## 7. 更新したいとき

公開後にファイルを直したい場合は、GitHubのリポジトリ画面で同じファイルを開き、鉛筆アイコンから編集できます。

編集後に **Commit changes** を押すと、しばらくして公開ページにも反映されます。
